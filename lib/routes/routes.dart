export 'pages.dart';
export 'names.dart';

//To keep it clean, if or where we call both of them above, we can simply  call this page instead of both of them listed above.
//Makes it simpler, cleaner and easier Thank God!